# TKD Hub — Telegram Bot & Mini App для Тхэквондо WT

## Обзор проекта

TKD Hub — платформа для спортсменов и тренеров по тхэквондо WT. Telegram Mini App (WebApp) + бот + веб-сайт. Основные функции: профили спортсменов/тренеров, каталог турниров с онлайн-заявками, дневник тренировок, рейтинговая система.

## Стек технологий

- **Backend:** Python 3.11+, aiogram 3.x (Telegram Bot API), FastAPI (API для Mini App и сайта)
- **Database:** PostgreSQL + SQLAlchemy (async) + Alembic (миграции)
- **Frontend (Mini App):** React + TypeScript + Tailwind CSS (или Vue.js)
- **Frontend (сайт):** тот же фреймворк или отдельный лендинг
- **Деплой:** Vercel
- **Язык интерфейса:** Русский + English (i18n с самого начала)

---

## Архитектура базы данных

### Таблица `users`
```
id: UUID (PK)
telegram_id: BIGINT (unique, not null)
username: VARCHAR(255) — telegram username
language: VARCHAR(2) — 'ru' / 'en'
created_at: TIMESTAMP
updated_at: TIMESTAMP
```

### Таблица `athletes` (спортсмены)
```
id: UUID (PK)
user_id: UUID (FK → users.id, unique)
full_name: VARCHAR(255)
date_of_birth: DATE
gender: VARCHAR(1) — 'M' / 'F'
weight_category: VARCHAR(50) — например "58kg", "63kg"
current_weight: DECIMAL(5,2) — актуальный вес
belt: VARCHAR(50) — "white", "yellow", "green", "blue", "red", "black_1dan"..."black_9dan"
country: VARCHAR(100)
city: VARCHAR(100)
club: VARCHAR(255) — nullable
photo_url: VARCHAR(500) — nullable
rating_points: INTEGER — default 0
is_active: BOOLEAN — default true
created_at: TIMESTAMP
updated_at: TIMESTAMP
```

### Таблица `coaches` (тренеры)
```
id: UUID (PK)
user_id: UUID (FK → users.id, unique)
full_name: VARCHAR(255)
date_of_birth: DATE
gender: VARCHAR(1) — 'M' / 'F'
country: VARCHAR(100)
city: VARCHAR(100)
club: VARCHAR(255)
qualification: VARCHAR(255) — например "4 дан, Мастер спорта"
photo_url: VARCHAR(500) — nullable
is_verified: BOOLEAN — default false (требует одобрения админа)
is_active: BOOLEAN — default true
created_at: TIMESTAMP
updated_at: TIMESTAMP
```

### Таблица `coach_athletes` (связь тренер ↔ спортсмен)
```
id: UUID (PK)
coach_id: UUID (FK → coaches.id)
athlete_id: UUID (FK → athletes.id)
status: VARCHAR(20) — 'pending' / 'active' / 'removed'
invited_at: TIMESTAMP
accepted_at: TIMESTAMP — nullable
```
UNIQUE(coach_id, athlete_id)

### Таблица `tournaments` (турниры)
```
id: UUID (PK)
name: VARCHAR(255)
description: TEXT — nullable
start_date: DATE
end_date: DATE
city: VARCHAR(100)
country: VARCHAR(100)
venue: VARCHAR(255) — название площадки
age_categories: JSONB — ["cadets", "juniors", "seniors"]
weight_categories: JSONB — по правилам WT
entry_fee: DECIMAL(10,2) — nullable
currency: VARCHAR(3) — 'USD', 'KGS', 'RUB' и т.д.
registration_deadline: DATE
organizer_contact: VARCHAR(255) — nullable
status: VARCHAR(20) — 'upcoming' / 'registration_open' / 'registration_closed' / 'ongoing' / 'completed'
importance_level: INTEGER — 1-5 (для расчёта рейтинга: 1=клубный, 5=международный)
created_by: UUID (FK → users.id) — кто добавил (админ или организатор)
created_at: TIMESTAMP
updated_at: TIMESTAMP
```

### Таблица `tournament_entries` (заявки на турниры)
```
id: UUID (PK)
tournament_id: UUID (FK → tournaments.id)
athlete_id: UUID (FK → athletes.id)
coach_id: UUID (FK → coaches.id) — кто заявил
weight_category: VARCHAR(50) — категория на момент заявки
age_category: VARCHAR(50)
status: VARCHAR(20) — 'pending' / 'approved' / 'rejected' / 'withdrawn'
created_at: TIMESTAMP
updated_at: TIMESTAMP
```
UNIQUE(tournament_id, athlete_id)

### Таблица `tournament_results` (результаты турниров)
```
id: UUID (PK)
tournament_id: UUID (FK → tournaments.id)
athlete_id: UUID (FK → athletes.id)
weight_category: VARCHAR(50)
age_category: VARCHAR(50)
place: INTEGER — 1, 2, 3 (два третьих места в тхэквондо)
rating_points_earned: INTEGER
created_at: TIMESTAMP
```

### Таблица `training_log` (дневник тренировок)
```
id: UUID (PK)
athlete_id: UUID (FK → athletes.id)
date: DATE
type: VARCHAR(50) — 'technique' / 'sparring' / 'poomsae' / 'physical' / 'other'
duration_minutes: INTEGER
intensity: VARCHAR(20) — 'light' / 'medium' / 'high'
weight: DECIMAL(5,2) — nullable
notes: TEXT — nullable
coach_comment: TEXT — nullable
created_at: TIMESTAMP
updated_at: TIMESTAMP
```

### Таблица `role_requests` (заявки на роль тренера)
```
id: UUID (PK)
user_id: UUID (FK → users.id)
requested_role: VARCHAR(20) — 'coach'
status: VARCHAR(20) — 'pending' / 'approved' / 'rejected'
admin_comment: TEXT — nullable
created_at: TIMESTAMP
reviewed_at: TIMESTAMP — nullable
reviewed_by: UUID (FK → users.id) — nullable
```

---

## Роли и права доступа

### Спортсмен (athlete)
- Просмотр и редактирование своего профиля
- Просмотр каталога турниров
- Отметка «Интерес» к турниру (тренер видит)
- Ведение дневника тренировок
- Просмотр рейтинга
- Просмотр своих результатов и истории турниров

### Тренер (coach)
- Всё, что может спортсмен (может иметь параллельно профиль спортсмена)
- Управление списком своих спортсменов (добавление через инвайт-ссылку)
- Подача заявок на турниры за своих спортсменов
- Редактирование заявок до дедлайна
- Просмотр дневников тренировок своих спортсменов
- Комментирование тренировок спортсменов

### Администратор (admin)
- Одобрение/отклонение заявок на роль тренера
- Добавление и редактирование турниров
- Внесение результатов турниров
- Управление рейтингом

---

## User Flow: Регистрация

### Шаг 0: /start
```
Бот: "🥋 Добро пожаловать в TKD Hub!\nВыберите язык / Choose language:"
Кнопки: [🇷🇺 Русский] [🇬🇧 English]
```

### Шаг 1: Выбор роли
```
Бот: "Выберите вашу роль:"
Кнопки: [🥋 Спортсмен] [🎓 Тренер]
```

### Шаг 2 (Спортсмен): Пошаговая регистрация
```
→ ФИО (текст)
→ Дата рождения (текст, формат ДД.ММ.ГГГГ)
→ Пол: [Мужской] [Женский]
→ Весовая категория: inline-кнопки (по стандартам WT)
→ Текущий вес (число, кг)
→ Пояс: inline-кнопки (10 гып → 9 дан)
→ Страна: inline-кнопки (топ-10 СНГ + "Другая")
→ Город (текст)
→ Клуб (текст или "Без клуба")
→ Фото: [Загрузить] [Пропустить]
→ "✅ Профиль создан!"
→ Открывается Mini App
```

### Шаг 2 (Тренер): Пошаговая регистрация
```
→ ФИО (текст)
→ Дата рождения (текст)
→ Пол: [Мужской] [Женский]
→ Страна
→ Город
→ Клуб / Организация (текст)
→ Квалификация (текст)
→ Фото: [Загрузить] [Пропустить]
→ "⏳ Заявка на роль тренера отправлена на проверку. Мы уведомим вас!"
→ После одобрения: "✅ Ваша заявка одобрена! Добро пожаловать!"
```

---

## User Flow: Основные вкладки Mini App

### Навигация (нижняя панель, 4 вкладки)
```
[🏆 Турниры] [📋 Дневник] [📊 Рейтинг] [👤 Профиль]
```

### Вкладка 1: Турниры 🏆

**Список турниров (главный экран вкладки):**
- Фильтры сверху: Страна | Возрастная группа | Дата
- Карточки турниров (как в Playbox Football):
  - Название, дата, город, статус (🟢 открыта / 🟡 скоро закроется / 🔴 завершён)
  - Количество заявленных спортсменов
  - Кнопка "Подробнее"

**Детальная страница турнира:**
- Полная информация: даты, место, категории, взнос, контакт организатора, дедлайн
- Для спортсмена: кнопка [⭐ Мне интересно]
- Для тренера: кнопка [📝 Заявить спортсменов]
  - Отображает список спортсменов тренера с чекбоксами
  - Кнопка [Подтвердить заявку (N спортсменов)]
- После завершения: вкладка "Результаты" с таблицей медалистов

**Мои заявки (подвкладка для тренера):**
- Список активных заявок с возможностью редактирования до дедлайна
- Статус каждой заявки

### Вкладка 2: Дневник 📋

**Спортсмен видит:**
- Календарь (месяц) с зелёными точками на днях с тренировками
- Кнопка [+ Добавить тренировку]
- При нажатии на день — список тренировок за этот день
- Статистика за месяц: количество тренировок, средняя интенсивность, динамика веса

**Добавление тренировки:**
- Дата: [Сегодня] [Вчера] [Выбрать]
- Тип: [Техника] [Спарринг] [Пхумсе] [Физика] [Другое]
- Длительность: [1ч] [1.5ч] [2ч] [Другое]
- Интенсивность: [Лёгкая] [Средняя] [Высокая]
- Вес (опционально): ввод числа
- Заметки (опционально): текст

**Тренер видит:**
- Выбор спортсмена из списка → его дневник
- Возможность оставить комментарий к тренировке
- Сводная статистика по спортсмену за период

### Вкладка 3: Рейтинг 📊

**Главный экран:**
- Фильтры: Регион (СНГ/Мир/Страна) | Весовая | Возрастная | Пол
- Подиум: топ-3 с фото (как в Playbox)
- Далее — список с позицией, именем, очками, городом
- Нажатие на спортсмена → его профиль

**Расчёт рейтинга:**
- Очки = f(место, importance_level турнира)
- Пример: 1 место на международном (×5) = 100 очков, 1 место на клубном (×1) = 20 очков
- Формулу уточним позже

### Вкладка 4: Профиль 👤

**Спортсмен:**
- Фото, ФИО, возраст (рассчитывается), пояс, весовая, текущий вес
- Клуб, город, тренер (если привязан)
- Статистика: турниры, медали (🥇🥈🥉), рейтинговые очки
- [📅 История турниров]
- [✏️ Редактировать профиль]
- [⚙️ Настройки]

**Тренер:**
- Фото, ФИО, квалификация, клуб
- [👥 Мои спортсмены (N)] → список с доступом к профилям
- [+ Добавить спортсмена] → инвайт-ссылка или поиск
- [📋 Активные заявки] → список заявок на турниры
- [✏️ Редактировать профиль]
- [⚙️ Настройки]

**Редактирование профиля:**
- Доступные поля для редактирования:
  - Вес / Весовая категория
  - Пояс
  - Клуб
  - Город
  - Фото
  - ФИО (с ограничением — не чаще 1 раза в месяц)

**Настройки:**
- Язык: Русский / English
- Уведомления: вкл/выкл по категориям (новые турниры, дедлайны, результаты)
- [Запросить роль тренера] (для спортсменов)
- [О боте / Поддержка]
- [Удалить аккаунт]

---

## Привязка спортсмена к тренеру

### Способ 1: Инвайт-ссылка
```
Тренер: [+ Добавить спортсмена] → [📎 Создать инвайт-ссылку]
→ Бот генерирует ссылку: t.me/tkd_hub_bot?start=invite_COACH_UUID
→ Тренер отправляет ссылку спортсмену
→ Спортсмен переходит:
  "Тренер Ким В.С. (Клуб 'Барс', Бишкек) приглашает вас в свою группу"
  [✅ Принять] [❌ Отклонить]
→ При принятии: связь создаётся (coach_athletes.status = 'active')
```

### Способ 2: Поиск (будущее)
```
Тренер: [+ Добавить спортсмена] → [🔍 Найти]
→ Поиск по имени / username
→ Отправка запроса → спортсмен подтверждает
```

---

## Уведомления (Telegram push)

- Спортсмену: "Тренер заявил вас на турнир X"
- Спортсмену: "Через 3 дня турнир X — не забудьте про взвешивание"
- Тренеру: "Спортсмен Y отметил интерес к турниру X"
- Тренеру: "До дедлайна заявки на турнир X осталось 2 дня"
- Тренеру: "Ваша заявка на роль тренера одобрена"
- Всем: "Результаты турнира X опубликованы"

---

## Структура проекта (файловая)

```
TKD_hub/
├── .env.example
├── PROJECT.md
├── README.md
│
├── bot/                        # Telegram bot (aiogram 3)
│   ├── __init__.py
│   ├── main.py                 # Entry point
│   ├── config.py               # Settings from env
│   ├── handlers/               # Message & callback handlers
│   │   ├── __init__.py
│   │   ├── start.py            # /start, выбор языка, роли
│   │   ├── registration.py     # Пошаговая регистрация (FSM)
│   │   ├── profile.py          # Профиль, редактирование
│   │   ├── tournaments.py      # Турниры (если без Mini App)
│   │   └── admin.py            # Админ-команды
│   ├── keyboards/              # Inline & reply keyboards
│   │   ├── __init__.py
│   │   ├── registration.py
│   │   ├── main_menu.py
│   │   └── profile.py
│   ├── states/                 # FSM states
│   │   ├── __init__.py
│   │   └── registration.py
│   ├── middlewares/             # Middleware (i18n, auth)
│   │   ├── __init__.py
│   │   └── i18n.py
│   └── utils/
│       ├── __init__.py
│       └── helpers.py
│
├── api/                        # FastAPI backend для Mini App
│   ├── __init__.py
│   ├── main.py
│   ├── routes/
│   │   ├── __init__.py
│   │   ├── athletes.py
│   │   ├── coaches.py
│   │   ├── tournaments.py
│   │   ├── training.py
│   │   ├── ratings.py
│   │   └── admin.py
│   ├── schemas/                # Pydantic models
│   │   ├── __init__.py
│   │   ├── athlete.py
│   │   ├── coach.py
│   │   ├── tournament.py
│   │   └── training.py
│   └── dependencies.py
│
├── db/                         # Database layer
│   ├── __init__.py
│   ├── base.py                 # SQLAlchemy base, engine, session
│   ├── models/                 # ORM models
│   │   ├── __init__.py
│   │   ├── user.py
│   │   ├── athlete.py
│   │   ├── coach.py
│   │   ├── tournament.py
│   │   ├── training.py
│   │   └── rating.py
│   └── migrations/             # Alembic
│       ├── env.py
│       └── versions/
│
├── webapp/                     # Telegram Mini App (React/Vue)
│   ├── package.json
│   ├── src/
│   │   ├── App.tsx
│   │   ├── pages/
│   │   │   ├── Tournaments.tsx
│   │   │   ├── TrainingLog.tsx
│   │   │   ├── Rating.tsx
│   │   │   └── Profile.tsx
│   │   ├── components/
│   │   └── api/
│   └── public/
│
└── locales/                    # Переводы
    ├── ru/
    │   └── messages.json
    └── en/
        └── messages.json
```

---

## Порядок разработки (фазы)

### Фаза 1: Фундамент (неделя 1-2)
- [ ] Настройка проекта: PostgreSQL + bot
- [ ] Модели базы данных + миграции
- [ ] Бот: /start → выбор языка → выбор роли → регистрация (FSM)
- [ ] Сохранение профиля в БД
- [ ] Базовые команды: /profile, /edit

### Фаза 2: Связи и турниры (неделя 3-4)
- [ ] Привязка спортсмен ↔ тренер (инвайт-ссылки)
- [ ] CRUD турниров (admin)
- [ ] Просмотр турниров (все)
- [ ] Заявки на турниры (тренеры)
- [ ] Уведомления

### Фаза 3: Mini App (неделя 5-7)
- [ ] FastAPI backend + API endpoints
- [ ] React Mini App: навигация, 4 вкладки
- [ ] Турниры — просмотр и заявки
- [ ] Профиль — просмотр и редактирование

### Фаза 4: Дневник и рейтинг (неделя 8-9)
- [ ] Дневник тренировок — CRUD
- [ ] Календарный вид
- [ ] Рейтинговая система — расчёт очков
- [ ] Страница рейтинга с фильтрами

### Фаза 5: Полировка и запуск (неделя 10-12)
- [ ] Результаты турниров
- [ ] Полный i18n
- [ ] Сайт-лендинг
- [ ] Тестирование с реальными тренерами
- [ ] Деплой на VPS

---

## Переменные окружения (.env)

```
BOT_TOKEN=your_telegram_bot_token
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/tkd_hub
ADMIN_IDS=123456789,987654321
WEBAPP_URL=https://your-domain.com/webapp
SECRET_KEY=your_secret_key
```

---

## Заметки

- Весовые категории по стандартам World Taekwondo (WT)
- Возрастные группы: Кадеты (12-14), Юниоры (15-17), Взрослые (18+), Ветераны (30+)
- В тхэквондо 2 бронзовых медалиста (place = 3 для двоих)
- Пояса: 10 гып (цветные) + дан (чёрные, 1-9)
- Рейтинг обнуляется в начале сезона или по скользящему окну (уточнить)
